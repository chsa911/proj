

        const createBMark

 position = String;
 colour = String;
 barcode = string;

        getBHoehe(e.target.value)   if BHoehe = 1
        then position = e,
        s
        getBBreite(e.target.value)  if BBreite = 1

        then colour = b,

        position+colour+barcode;


