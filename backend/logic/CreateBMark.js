import React, { useEffect, useState } from "react";


//Create a Bookmark
        const createBMark() {
//Variables that determine a Bookmark
 position = String;
 colour = String;
 barcode = string;
}
//Get Values for from Inputfield when creating a book
        getBHoehe(e.target.value)
        getBBreite(e.target.value)

//Check postion of BMar
         if( BHoehe = 1){
        then position = e,
        }else[

        ]

        if( BBreite = 1){
        then colour = b,
        }


        //Get barcode: Start with 1 rank 001 and check in database if value is unique

//Konkatenate Values to complete Bookmark
        position+colour+barcode;
// Pass Booksmark to Inputfield in CreateBooks
        setBMark;
