import React, { useEffect, useState } from "react";


//Create a Bookmark
        const createBMark() {
//Variables that determine a Bookmark
//position depends on the BHohe and BBreite

 position = String;
 colour = String;
 barcode = string;
}
//Get Values from Inputfield when creating a book
        const [BHoehe, setBHoehe] = useState('');
        const [BBreite, setBBreite] = useState('');
console.log ('BHoehe');

//GEt postion and colour of BMark
         if( BBreite <= 10,5 and BHoehe <= 17,5 ){
        then position = e,
        and colour = b;
        }else if { BBreite <= 10,5 and BHoehe >17,5 {
        then position ist o, and colour =b;
        }

        }
//Konkatenate Values to complete Bookmark
let barcode = 001;

let BMArk = position+colour+barcode;

//Uniquenesstest of BMark in DB If BMark is already there then change to next ranked barcode:
        const [BMark, setBMark] = useState('');






// Pass Booksmark to Inputfield in CreateBooks
        setBMark;
